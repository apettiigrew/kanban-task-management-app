# Database (Supabase)
# Connect to Supabase via connection pooling
DATABASE_URL=

# Direct connection to the database. Used for migrations
DIRECT_URL=
# Next.js
NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000"

# AWS Cognito (values from Serverless output: UserPoolId, UserPoolWebClientId)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
AWS_REGION=

NODE_ENV="development"

OPENAI_API_KEY=
